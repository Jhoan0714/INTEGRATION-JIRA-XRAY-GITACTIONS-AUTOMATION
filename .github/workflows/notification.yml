on: push
name: Slack Notification Demo
jobs:
    slackNotification:
      name: Slack Notification Demo
      runs-on: ubuntu-latest
      steps:
      - uses: actions/checkout@master
      - name: Slack Notification Demo
        uses: bryannice/gitactions-slack-notification@master
        env:
          SLACK_WEBHOOK: ${{ SLACK_BOT_USER_OAUTH_ACCESS_TOKEN }}
          SLACK_TITLE: 'Slack Notification Demo'
          SLACK_CHANNEL: 'aut-team'
          SLACK_MESSAGE: 'Demo''ing the Slack Notification'
          SLACK_COLOR: '#efefef'
          SLACK_ICON: 'https://github.com/<Github User>.png?size=48'
#jobs:
#  slack-notification:
#    runs-on: ubuntu-latest
#    name: Sends a message to Slack when a push, a pull request or an issue is made
#    steps:
#      - name: Send message to Slack API
#        uses: archive/github-actions-slack@master
#        with:
#          slack-bot-user-oauth-access-token: ${{ secrets.SLACK_BOT_USER_OAUTH_ACCESS_TOKEN }}
#          slack-channel: aut-team
#          slack-text: "Slack Notification in Git Action Test :nerd_face:"
#          slack-color: red
          
#name: Notification PR in Slack
#on:
#  pull_request:
#    types: [opened]
#jobs:
#  slackNotification:
#    name: Slack Notification
#    runs-on: ubuntu-latest
#    steps:
#      - name: Slack Notification
#        uses: rtCamp/action-slack-notify@master
#        env:
#          SLACK_CHANNEL: "#aut-team"
#          SLACK_COLOR: "good"
#          SLACK_ICON_EMOJI: ":github:"
#          SLACK_TITLE: "Slack Notification"
#          SLACK_MESSAGE: "https://github.com/lendingfront/automated-tests/pull/${{github.event.number}} \n ${{github.event.comment.body}}"
#          SLACK_USERNAME: "PR - Automation Team"
#          SLACK_FOOTER: "${GITHUB_REPOSITORY}"
